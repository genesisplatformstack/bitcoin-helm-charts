apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "public-pool.fullname" . }}-test-connection"
  labels:
{{ include "public-pool.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "public-pool.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
